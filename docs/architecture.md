# Architecture

Separate curation, installation and runtime loading. Broad curation; pinned installs; tiny task-routed runtime context. Hermes is the preferred global orchestrator; specialists never become competing control planes.
