# Memory

Default to project-local JSONL/Markdown with retrieval. Add vector/graph memory only when measured needs justify it. Store scope, provenance, timestamp, confidence, staleness/expiry and tags.
