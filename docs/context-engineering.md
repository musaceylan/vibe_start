# Context Engineering

Exploration order: git status → tree/map → rg/find → symbol → AST → LSP → selected ranges/files → deterministic repo map → semantic/code graph → LLM-heavy analysis. Escalate only when cheaper tiers fail.
