# Hooks

ALWAYS: cheap deterministic secret/destructive guards. CONDITIONAL: security review, context compression, browser QA. MANUAL: deep architecture audits/heavy semantic indexing. Hooks need measurable benefit, bounded runtime and safe failure behavior.
