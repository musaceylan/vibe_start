# Community and ecosystem discovery — 2026-08-26

This pass expands the seed curation with community-discovered candidates and then verifies that the major candidates exist on GitHub. Community popularity is discovery evidence only; promotion still requires license/provenance/security/benchmark checks.

## Promote to curated active/specialist

- `obra/superpowers` — use composable planning, TDD, debugging, review and verification procedures; never a competing global orchestrator.
- `JuliusBrussee/caveman` — use the MIT skill; treat the proxy as opt-in until local token, correctness, privacy and security benchmarks pass.
- `oraios/serena` — semantic symbol/LSP navigation before broad file reads.
- `snyk/agent-scan` — security gate for third-party skills, MCP servers and agent configs.
- `upstash/context7` — current external library/framework docs, task-scoped.
- `vercel-labs/agent-browser` — low-context browser interaction; Playwright remains deterministic E2E fallback.
- `openrewrite/rewrite` — deterministic large-scale migrations/refactoring.
- `Orchestra-Research/AI-Research-SKILLs` — AI/ML specialist source; never bulk-load.

## Benchmark / candidate

- `tirth8205/code-review-graph` — compare code-graph/blast-radius value against Serena + repomap.
- `gastownhall/beads` — compare durable task state against simple project-local memory.
- `OthmanAdi/planning-with-files` — evaluate as lightweight working memory for long tasks.
- `mvanhorn/last30days-skill` — evaluate as one input to `vibe update`; results remain quarantined.

## Routing changes

- Repo understanding: `rg` → Serena/symbols → ast-grep → code-review-graph/repomap → deep intelligence.
- Refactoring: local patch → ast-grep → OpenRewrite/language codemod → tests + review.
- Security: agent-scan at curation/install boundary + deterministic scanners + specialist review.
- Browser: agent-browser for cheap interaction; Playwright for full deterministic E2E/visual QA.
- External docs: Context7 only when version-specific library knowledge is needed.

## Control-plane rule

Hermes remains the global classifier/orchestrator. Superpowers, repo-harness, security specialists, UI specialists, refactoring engines and context tools are capabilities routed by task.
