#!/usr/bin/env bash
set -euo pipefail
echo "Discovery is proposal-only: collect metadata; never execute candidate code. Gates: provenance activity docs license security overlap portability context-cost benchmark."
